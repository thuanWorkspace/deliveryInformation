/*
All material copyright ESRI, All Rights Reserved, unless otherwise specified.
See https://js.arcgis.com/4.28/esri/copyright.txt for details.
*/
define(["exports","../views/3d/webgl-engine/core/shaderLibrary/ScreenSpacePass.glsl","../views/3d/webgl-engine/core/shaderModules/interfaces","../views/3d/webgl-engine/core/shaderModules/ShaderBuilder","../views/3d/webgl-engine/core/shaderModules/Texture2DDrawUniform"],(function(e,r,i,l,t){"use strict";class n extends i.NoParameters{}function o(){const e=new l.ShaderBuilder,{outputs:n,fragment:o}=e;return e.include(r.ScreenSpacePass),o.uniforms.add(new t.Texture2DDrawUniform("textureInput",(e=>e.input))),o.constants.add("outlineSize","int",Math.ceil(d/2)),n.add("fragGrid","vec2"),o.code.add(i.glsl`
    void main() {
      float red = 0.0;
      float green = 1.0;
      int cellSize = ${i.glsl.int(a)};
      vec2 texelSize = 1.0 / vec2(textureSize(textureInput, 0));
      vec2 offset = floor(gl_FragCoord.xy) * vec2(float(cellSize));

      for(int x = -outlineSize; x < cellSize + outlineSize; x += 2) {
        for(int y = -outlineSize; y < cellSize + outlineSize; y += 2) {
          vec2 coord = (offset + vec2(float(x), float(y))) * texelSize;
          vec4 value = texture(textureInput, coord);
          float mx = floor(max(value.g, value.b));

          red = max(red, ceil(value.r));
          green = min(green, mx);
          if(red == 1.0 && green == 0.0) {
            fragGrid = vec2(red, green);
            return;
          }
        }
      }
      fragGrid = vec2(red, green);
    }`),e}const a=32,d=9,u=.4,s=Object.freeze(Object.defineProperty({__proto__:null,HighlightDownsampleDrawParameters:n,blurSize:u,build:o,gridCellPixelSize:a,outlineSize:d},Symbol.toStringTag,{value:"Module"}));e.HighlightDownsample=s,e.HighlightDownsampleDrawParameters=n,e.blurSize=u,e.build=o,e.gridCellPixelSize=a,e.outlineSize=d}));
