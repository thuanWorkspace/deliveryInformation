/*
All material copyright ESRI, All Rights Reserved, unless otherwise specified.
See https://js.arcgis.com/4.28/esri/copyright.txt for details.
*/
define(["exports","../views/3d/webgl-engine/core/shaderLibrary/ForwardLinearDepth.glsl","../views/3d/webgl-engine/core/shaderLibrary/ShaderOutput","../views/3d/webgl-engine/core/shaderLibrary/Slice.glsl","../views/3d/webgl-engine/core/shaderLibrary/attributes/ObjectAndLayerIdColor.glsl","../views/3d/webgl-engine/core/shaderLibrary/attributes/RibbonVertexPosition.glsl","../views/3d/webgl-engine/core/shaderLibrary/output/OutputDepth.glsl","../views/3d/webgl-engine/core/shaderLibrary/shading/LineStipple.glsl","../views/3d/webgl-engine/core/shaderLibrary/shading/MarkerSizing.glsl","../views/3d/webgl-engine/core/shaderLibrary/shading/MultipassTerrainTest.glsl","../views/3d/webgl-engine/core/shaderLibrary/shading/PiUtils.glsl","../views/3d/webgl-engine/core/shaderLibrary/util/AlphaCutoff","../views/3d/webgl-engine/core/shaderLibrary/util/ColorConversion.glsl","../views/3d/webgl-engine/core/shaderLibrary/util/View.glsl","../views/3d/webgl-engine/core/shaderModules/Float2PassUniform","../views/3d/webgl-engine/core/shaderModules/Float4PassUniform","../views/3d/webgl-engine/core/shaderModules/FloatPassUniform","../views/3d/webgl-engine/core/shaderModules/interfaces","../views/3d/webgl-engine/core/shaderModules/Matrix4PassUniform","../views/3d/webgl-engine/core/shaderModules/ShaderBuilder","../views/3d/webgl-engine/lib/TransparencyPassType","../views/3d/webgl-engine/lib/VertexAttribute","../views/3d/webgl-engine/shaders/LineMarkerTechniqueConfiguration","../views/3d/webgl-engine/shaders/RibbonLineTechniqueConfiguration"],(function(e,i,t,n,o,r,l,a,s,d,p,c,g,v,f,u,m,h,S,b,D,x,L,w){"use strict";const y=1;function C(e){const C=new b.ShaderBuilder,{vertex:P,fragment:A}=C,R=e.multipassEnabled&&(e.output===t.ShaderOutput.Color||e.output===t.ShaderOutput.Alpha);C.include(p.PiUtils),C.include(r.RibbonVertexPosition,e),C.include(a.LineStipple,e);const F=e.applyMarkerOffset&&!e.draped;F&&(P.uniforms.add(new m.FloatPassUniform("markerScale",(e=>e.markerScale))),C.include(s.MarkerSizing,{space:L.LineMarkerSpace.World,draped:!1})),e.output===t.ShaderOutput.Depth&&C.include(l.OutputDepth,e),C.include(o.ObjectAndLayerIdColor,e),v.addProjViewLocalOrigin(P,e),P.uniforms.add(new S.Matrix4PassUniform("inverseProjectionMatrix",((e,i)=>i.camera.inverseProjectionMatrix)),new f.Float2PassUniform("nearFar",((e,i)=>i.camera.nearFar)),new m.FloatPassUniform("miterLimit",(e=>"miter"!==e.join?0:e.miterLimit)),new u.Float4PassUniform("viewport",((e,i)=>i.camera.fullViewport))),P.constants.add("LARGE_HALF_FLOAT","float",65500),C.attributes.add(x.VertexAttribute.POSITION,"vec3"),C.attributes.add(x.VertexAttribute.SUBDIVISIONFACTOR,"float"),C.attributes.add(x.VertexAttribute.UV0,"vec2"),C.attributes.add(x.VertexAttribute.AUXPOS1,"vec3"),C.attributes.add(x.VertexAttribute.AUXPOS2,"vec3"),C.varyings.add("vColor","vec4"),C.varyings.add("vpos","vec3"),i.addLinearDepth(C),R&&C.varyings.add("depth","float");const O=e.stippleEnabled;O&&C.varyings.add("vLineSizeInv","float");const T=e.capType===w.CapType.ROUND,z=O||T;z&&C.varyings.add("vLineWidth","float");const j=e.innerColorEnabled||T;j&&C.varyings.add("vLineDistance","float");const E=e.stippleEnabled&&T,V=e.falloffEnabled||E;V&&C.varyings.add("vLineDistanceNorm","float"),T&&(C.varyings.add("vSegmentSDF","float"),C.varyings.add("vReverseSegmentSDF","float")),P.code.add(h.glsl`#define PERPENDICULAR(v) vec2(v.y, -v.x);
float interp(float ncp, vec4 a, vec4 b) {
return (-ncp - a.z) / (b.z - a.z);
}
vec2 rotate(vec2 v, float a) {
float s = sin(a);
float c = cos(a);
mat2 m = mat2(c, -s, s, c);
return m * v;
}`),P.code.add(h.glsl`vec4 projectAndScale(vec4 pos) {
vec4 posNdc = proj * pos;
posNdc.xy *= viewport.zw / posNdc.w;
return posNdc;
}`),i.addCalculateLinearDepth(C),P.code.add(h.glsl`
    void clipAndTransform(inout vec4 pos, inout vec4 prev, inout vec4 next, in bool isStartVertex) {
      float vnp = nearFar[0] * 0.99;

      if(pos.z > -nearFar[0]) {
        //current pos behind ncp --> we need to clip
        if (!isStartVertex) {
          if(prev.z < -nearFar[0]) {
            //previous in front of ncp
            pos = mix(prev, pos, interp(vnp, prev, pos));
            next = pos;
          } else {
            pos = vec4(0.0, 0.0, 0.0, 1.0);
          }
        } else {
          if(next.z < -nearFar[0]) {
            //next in front of ncp
            pos = mix(pos, next, interp(vnp, pos, next));
            prev = pos;
          } else {
            pos = vec4(0.0, 0.0, 0.0, 1.0);
          }
        }
      } else {
        //current position visible
        if (prev.z > -nearFar[0]) {
          //previous behind ncp
          prev = mix(pos, prev, interp(vnp, pos, prev));
        }
        if (next.z > -nearFar[0]) {
          //next behind ncp
          next = mix(next, pos, interp(vnp, next, pos));
        }
      }

      ${R?"depth = pos.z;":""}
      linearDepth = calculateLinearDepth(nearFar,pos.z);

      pos = projectAndScale(pos);
      next = projectAndScale(next);
      prev = projectAndScale(prev);
    }
  `),v.addPixelRatio(P),P.code.add(h.glsl`
  void main(void) {
    // unpack values from uv0.y
    bool isStartVertex = abs(abs(uv0.y)-3.0) == 1.0;

    float coverage = 1.0;

    // Check for special value of uv0.y which is used by the Renderer when graphics
    // are removed before the VBO is recompacted. If this is the case, then we just
    // project outside of clip space.
    if (uv0.y == 0.0) {
      // Project out of clip space
      gl_Position = vec4(1e038, 1e038, 1e038, 1.0);
    }
    else {
      bool isJoin = abs(uv0.y) < 3.0;

      float lineSize = getSize();
      float lineWidth = lineSize * pixelRatio;

      ${z?h.glsl`vLineWidth = lineWidth;`:""}
      ${O?h.glsl`vLineSizeInv = 1.0 / lineSize;`:""}

      // convert sub-pixel coverage to alpha
      if (lineWidth < 1.0) {
        coverage = lineWidth;
        lineWidth = 1.0;
      }else{
        // Ribbon lines cannot properly render non-integer sizes. Round width to integer size if
        // larger than one for better quality. Note that we do render < 1 pixels more or less correctly
        // so we only really care to round anything larger than 1.
        lineWidth = floor(lineWidth + 0.5);
      }

      vec4 pos  = view * vec4(position.xyz, 1.0);
      vec4 prev = view * vec4(auxpos1.xyz, 1.0);
      vec4 next = view * vec4(auxpos2.xyz, 1.0);
  `),F&&P.code.add(h.glsl`vec4 other = isStartVertex ? next : prev;
bool markersHidden = areWorldMarkersHidden(pos, other);
if(!isJoin && !markersHidden) {
pos.xyz += normalize(other.xyz - pos.xyz) * getWorldMarkerSize(pos) * 0.5;
}`),P.code.add(h.glsl`clipAndTransform(pos, prev, next, isStartVertex);
vec2 left = (pos.xy - prev.xy);
vec2 right = (next.xy - pos.xy);
float leftLen = length(left);
float rightLen = length(right);`);(e.stippleEnabled||T)&&P.code.add(h.glsl`
      float isEndVertex = float(!isStartVertex);
      vec2 segmentOrigin = mix(pos.xy, prev.xy, isEndVertex);
      vec2 segment = mix(right, left, isEndVertex);
      ${T?h.glsl`vec2 segmentEnd = mix(next.xy, pos.xy, isEndVertex);`:""}
    `),P.code.add(h.glsl`left = (leftLen > 0.001) ? left/leftLen : vec2(0.0, 0.0);
right = (rightLen > 0.001) ? right/rightLen : vec2(0.0, 0.0);
vec2 capDisplacementDir = vec2(0, 0);
vec2 joinDisplacementDir = vec2(0, 0);
float displacementLen = lineWidth;
if (isJoin) {
bool isOutside = (left.x * right.y - left.y * right.x) * uv0.y > 0.0;
joinDisplacementDir = normalize(left + right);
joinDisplacementDir = PERPENDICULAR(joinDisplacementDir);
if (leftLen > 0.001 && rightLen > 0.001) {
float nDotSeg = dot(joinDisplacementDir, left);
displacementLen /= length(nDotSeg * left - joinDisplacementDir);
if (!isOutside) {
displacementLen = min(displacementLen, min(leftLen, rightLen)/abs(nDotSeg));
}
}
if (isOutside && (displacementLen > miterLimit * lineWidth)) {`),e.roundJoins?P.code.add(h.glsl`
        vec2 startDir = leftLen < 0.001 ? right : left;
        startDir = PERPENDICULAR(startDir);

        vec2 endDir = rightLen < 0.001 ? left : right;
        endDir = PERPENDICULAR(endDir);

        float factor = ${e.stippleEnabled?h.glsl`min(1.0, subdivisionFactor * ${h.glsl.float((y+2)/(y+1))})`:h.glsl`subdivisionFactor`};

        float rotationAngle = acos(clamp(dot(startDir, endDir), -1.0, 1.0));
        joinDisplacementDir = rotate(startDir, -sign(uv0.y) * factor * rotationAngle);
      `):P.code.add(h.glsl`if (leftLen < 0.001) {
joinDisplacementDir = right;
}
else if (rightLen < 0.001) {
joinDisplacementDir = left;
}
else {
joinDisplacementDir = (isStartVertex || subdivisionFactor > 0.0) ? right : left;
}
joinDisplacementDir = PERPENDICULAR(joinDisplacementDir);`);const W=e.capType!==w.CapType.BUTT;return P.code.add(h.glsl`
        displacementLen = lineWidth;
      }
    } else {
      // CAP handling ---------------------------------------------------
      joinDisplacementDir = isStartVertex ? right : left;
      joinDisplacementDir = PERPENDICULAR(joinDisplacementDir);

      ${W?h.glsl`capDisplacementDir = isStartVertex ? -right : left;`:""}
    }
  `),P.code.add(h.glsl`
    // Displacement (in pixels) caused by join/or cap
    vec2 dpos = joinDisplacementDir * sign(uv0.y) * displacementLen + capDisplacementDir * displacementLen;

    ${V||j?h.glsl`float lineDistNorm = sign(uv0.y) * pos.w;`:""}

    ${j?h.glsl`vLineDistance = lineWidth * lineDistNorm;`:""}
    ${V?h.glsl`vLineDistanceNorm = lineDistNorm;`:""}

    pos.xy += dpos;
  `),T&&P.code.add(h.glsl`vec2 segmentDir = normalize(segment);
vSegmentSDF = (isJoin && isStartVertex) ? LARGE_HALF_FLOAT : (dot(pos.xy - segmentOrigin, segmentDir) * pos.w) ;
vReverseSegmentSDF = (isJoin && !isStartVertex) ? LARGE_HALF_FLOAT : (dot(pos.xy - segmentEnd, -segmentDir) * pos.w);`),e.stippleEnabled&&(e.draped?P.uniforms.add(new m.FloatPassUniform("worldToScreenRatio",((e,i)=>1/i.screenToPCSRatio))):P.code.add(h.glsl`vec3 segmentCenter = mix((auxpos2 + position) * 0.5, (position + auxpos1) * 0.5, isEndVertex);
float worldToScreenRatio = computeWorldToScreenRatio(segmentCenter);`),P.code.add(h.glsl`float segmentLengthScreenDouble = length(segment);
float segmentLengthScreen = segmentLengthScreenDouble * 0.5;
float discreteWorldToScreenRatio = discretizeWorldToScreenRatio(worldToScreenRatio);
float segmentLengthRender = length(mix(auxpos2 - position, position - auxpos1, isEndVertex));
vStipplePatternStretch = worldToScreenRatio / discreteWorldToScreenRatio;`),e.draped?P.code.add(h.glsl`float segmentLengthPseudoScreen = segmentLengthScreen / pixelRatio * discreteWorldToScreenRatio / worldToScreenRatio;
float startPseudoScreen = uv0.x * discreteWorldToScreenRatio - mix(0.0, segmentLengthPseudoScreen, isEndVertex);`):P.code.add(h.glsl`float startPseudoScreen = mix(uv0.x, uv0.x - segmentLengthRender, isEndVertex) * discreteWorldToScreenRatio;
float segmentLengthPseudoScreen = segmentLengthRender * discreteWorldToScreenRatio;`),P.uniforms.add(new m.FloatPassUniform("stipplePatternPixelSize",(e=>a.computePixelSize(e)))),P.code.add(h.glsl`float patternLength = lineSize * stipplePatternPixelSize;
vStippleDistanceLimits = computeStippleDistanceLimits(startPseudoScreen, segmentLengthPseudoScreen, segmentLengthScreen, patternLength);
vStippleDistance = mix(vStippleDistanceLimits.x, vStippleDistanceLimits.y, isEndVertex);
if (segmentLengthScreenDouble >= 0.001) {
vec2 stippleDisplacement = pos.xy - segmentOrigin;
float stippleDisplacementFactor = dot(segment, stippleDisplacement) / (segmentLengthScreenDouble * segmentLengthScreenDouble);
vStippleDistance += (stippleDisplacementFactor - isEndVertex) * (vStippleDistanceLimits.y - vStippleDistanceLimits.x);
}
vStippleDistanceLimits *= pos.w;
vStippleDistance *= pos.w;
vStippleDistanceLimits = isJoin ?
vStippleDistanceLimits :
isStartVertex ?
vec2(-1e34, vStippleDistanceLimits.y) :
vec2(vStippleDistanceLimits.x, 1e34);`)),P.code.add(h.glsl`
      // Convert back into NDC
      pos.xy = (pos.xy / viewport.zw) * pos.w;

      vColor = getColor();
      vColor.a *= coverage;

      ${e.wireframe&&!e.draped?"pos.z -= 0.001 * pos.w;":""}

      // transform final position to camera space for slicing
      vpos = (inverseProjectionMatrix * pos).xyz;
      gl_Position = pos;
      forwardObjectAndLayerIdColor();
    }
  }
  `),R&&C.include(d.multipassTerrainTest,e),C.include(n.SliceDraw,e),A.include(g.ColorConversion),A.code.add(h.glsl`
  void main() {
    discardBySlice(vpos);
    ${R?"terrainDepthTest(depth);":""}
  `),e.wireframe?A.code.add(h.glsl`vec4 finalColor = vec4(1.0, 0.0, 1.0, 1.0);`):(T&&A.code.add(h.glsl`
      float sdf = min(vSegmentSDF, vReverseSegmentSDF);
      vec2 fragmentPosition = vec2(
        min(sdf, 0.0),
        vLineDistance
      ) * gl_FragCoord.w;

      float fragmentRadius = length(fragmentPosition);
      float fragmentCapSDF = (fragmentRadius - vLineWidth) * 0.5; // Divide by 2 to transform from double pixel scale
      float capCoverage = clamp(0.5 - fragmentCapSDF, 0.0, 1.0);

      if (capCoverage < ${h.glsl.float(c.symbolAlphaCutoff)}) {
        discard;
      }
    `),E?A.code.add(h.glsl`
      vec2 stipplePosition = vec2(
        min(getStippleSDF() * 2.0 - 1.0, 0.0),
        vLineDistanceNorm * gl_FragCoord.w
      );
      float stippleRadius = length(stipplePosition * vLineWidth);
      float stippleCapSDF = (stippleRadius - vLineWidth) * 0.5; // Divide by 2 to transform from double pixel scale
      float stippleCoverage = clamp(0.5 - stippleCapSDF, 0.0, 1.0);
      float stippleAlpha = step(${h.glsl.float(c.symbolAlphaCutoff)}, stippleCoverage);
      `):A.code.add(h.glsl`float stippleAlpha = getStippleAlpha();`),e.output!==t.ShaderOutput.ObjectAndLayerIdColor&&A.code.add(h.glsl`discardByStippleAlpha(stippleAlpha, stippleAlphaColorDiscard);`),A.uniforms.add(new u.Float4PassUniform("intrinsicColor",(e=>e.color))),A.code.add(h.glsl`vec4 color = intrinsicColor * vColor;`),e.innerColorEnabled&&(A.uniforms.add(new u.Float4PassUniform("innerColor",(e=>e.innerColor??e.color)),new m.FloatPassUniform("innerWidth",((e,i)=>e.innerWidth*i.camera.pixelRatio))),A.code.add(h.glsl`float distToInner = abs(vLineDistance * gl_FragCoord.w) - innerWidth;
float innerAA = clamp(0.5 - distToInner, 0.0, 1.0);
float innerAlpha = innerColor.a + color.a * (1.0 - innerColor.a);
color = mix(color, vec4(innerColor.rgb, innerAlpha), innerAA);`)),A.code.add(h.glsl`vec4 finalColor = blendStipple(color, stippleAlpha);`),e.falloffEnabled&&(A.uniforms.add(new m.FloatPassUniform("falloff",(e=>e.falloff))),A.code.add(h.glsl`finalColor.a *= pow(max(0.0, 1.0 - abs(vLineDistanceNorm * gl_FragCoord.w)), falloff);`))),A.code.add(h.glsl`
    ${e.output===t.ShaderOutput.ObjectAndLayerIdColor?h.glsl`finalColor.a = 1.0;`:""}

    if (finalColor.a < ${h.glsl.float(c.symbolAlphaCutoff)}) {
      discard;
    }

    ${e.output===t.ShaderOutput.Alpha?h.glsl`fragColor = vec4(finalColor.a);`:""}
    ${e.output===t.ShaderOutput.Color?h.glsl`fragColor = highlightSlice(finalColor, vpos);`:""}
    ${e.output===t.ShaderOutput.Color&&e.transparencyPassType===D.TransparencyPassType.Color?"fragColor = premultiplyAlpha(fragColor);":""}
    ${e.output===t.ShaderOutput.Highlight?h.glsl`fragColor = vec4(1.0);`:""}
    ${e.output===t.ShaderOutput.Depth?h.glsl`outputDepth(linearDepth);`:""}
    ${e.output===t.ShaderOutput.ObjectAndLayerIdColor?h.glsl`outputObjectAndLayerIdColor();`:""}
  }
  `),C}const P=Object.freeze(Object.defineProperty({__proto__:null,build:C,ribbonlineNumRoundJoinSubdivisions:y},Symbol.toStringTag,{value:"Module"}));e.RibbonLine=P,e.build=C,e.ribbonlineNumRoundJoinSubdivisions=y}));
