/*
All material copyright ESRI, All Rights Reserved, unless otherwise specified.
See https://js.arcgis.com/4.28/esri/copyright.txt for details.
*/
define(["exports","../../../core/shaderLibrary/util/RgbaFloatEncoding.glsl","../../../core/shaderModules/FloatDrawUniform","../../../core/shaderModules/interfaces","../../../core/shaderModules/Texture2DDrawUniform","./EdgeUtil.glsl","./UnpackAttributes.glsl"],(function(e,t,r,a,s,l,o){"use strict";class u extends a.NoParameters{}function n(e,u){e.include(o.UnpackAttributes,u);const{vertex:n,fragment:c}=e;switch(l.usesSketchLogic(u)&&(n.uniforms.add(new s.Texture2DDrawUniform("strokesTexture",(e=>e.strokesTexture.texture))),n.uniforms.add(new r.FloatDrawUniform("strokesLog2Resolution",(e=>Math.log2(e.strokesTexture.resolution))),new r.FloatDrawUniform("strokeVariants",(e=>e.strokesTexture.variants))),e.varyings.add("vStrokeUV","vec2"),c.uniforms.add(new s.Texture2DDrawUniform("strokesTexture",(e=>e.strokesTexture.texture)),new r.FloatDrawUniform("strokesNormalizationScale",(e=>e.strokesTexture.normalizationScale))),n.code.add(a.glsl`void calculateStyleOutputsSketch(float lineLength, UnpackedAttributes unpackedAttributes) {
vec2 sidenessNorm = unpackedAttributes.sidenessNorm;
float lineIndex = clamp(ceil(log2(lineLength)), 0.0, strokesLog2Resolution);
vStrokeUV = vec2(exp2(lineIndex) * sidenessNorm.y, lineIndex * strokeVariants + variantStroke + 0.5) / vec2(textureSize(strokesTexture, 0));
vStrokeUV.x += variantOffset;
}`),e.fragment.include(t.RgbaFloatEncoding),c.code.add(a.glsl`float calculateLineOffsetSketch() {
float offsetNorm = rgba2float(texture(strokesTexture, vStrokeUV));
return (offsetNorm - 0.5) * strokesNormalizationScale;
}
float calculateLinePressureSketch() {
return rgba2float(texture(strokesTexture, vStrokeUV + vec2(0.0, 0.5)));
}`)),u.mode){case l.EdgeUtilMode.SOLID:n.code.add(a.glsl`void calculateStyleOutputs(UnpackedAttributes unpackedAttributes) {}`),c.code.add(a.glsl`float calculateLineOffset() {
return 0.0;
}
float calculateLinePressure() {
return 1.0;
}`);break;case l.EdgeUtilMode.SKETCH:n.code.add(a.glsl`void calculateStyleOutputs(UnpackedAttributes unpackedAttributes)
{
calculateStyleOutputsSketch(vLineLengthPixels, unpackedAttributes);
}`),c.code.add(a.glsl`float calculateLineOffset() {
return calculateLineOffsetSketch();
}
float calculateLinePressure() {
return calculateLinePressureSketch();
}`);break;case l.EdgeUtilMode.MIXED:e.varyings.add("vType","float"),n.code.add(a.glsl`void calculateStyleOutputs(UnpackedAttributes unpackedAttributes)
{
vType = unpackedAttributes.type;
if (unpackedAttributes.type <= 0.0) {
calculateStyleOutputsSketch(vLineLengthPixels, unpackedAttributes);
}
}`),c.code.add(a.glsl`float calculateLineOffset() {
if (vType <= 0.0) {
return calculateLineOffsetSketch();
}
else {
return 0.0;
}
}
float calculateLinePressure() {
if (vType <= 0.0) {
return calculateLinePressureSketch();
}
else {
return 1.0;
}
}`)}}e.LineOffset=n,e.LineOffsetDrawParameters=u,Object.defineProperty(e,Symbol.toStringTag,{value:"Module"})}));
